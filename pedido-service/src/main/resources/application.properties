spring.profiles.active=${SPRING_PROFILE:neon}
server.port=${PORT:8083}
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
